import { StyleSheet } from "react-native-web";

export const styles = StyleSheet.create({
    input: {
        padding: 8,
        // width:200,
        paddingHorizontal: 60,
        paddingVertical: 8,
        borderColor: 'grey',
        borderWidth: 1,
        marginTop: 20,
        borderRadius: 4
      }
});